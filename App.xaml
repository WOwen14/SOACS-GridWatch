<Application x:Class="SOACS.GridWatch.App" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" Startup="Application_Startup">
  <Application.Resources>
    <SolidColorBrush x:Key="Bg" Color="#050B10"/>
    <SolidColorBrush x:Key="Panel" Color="#071722"/>
    <SolidColorBrush x:Key="PanelAlt" Color="#082232"/>
    <SolidColorBrush x:Key="Cyan" Color="#00E5FF"/>
    <SolidColorBrush x:Key="Text" Color="#D8FBFF"/>

    <Style TargetType="Button">
      <Setter Property="Margin" Value="4"/>
      <Setter Property="Padding" Value="12,6"/>
      <Setter Property="Foreground" Value="#D8FBFF"/>
      <Setter Property="Background" Value="#071722"/>
      <Setter Property="BorderBrush" Value="#00E5FF"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="FontFamily" Value="Consolas"/>
      <Setter Property="FontWeight" Value="Bold"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="Button">
            <Border x:Name="Bd" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" CornerRadius="8" Padding="{TemplateBinding Padding}">
              <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Bd" Property="Background" Value="#0D2B3A"/></Trigger>
              <Trigger Property="IsPressed" Value="True"><Setter TargetName="Bd" Property="BorderBrush" Value="#33FF85"/></Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style TargetType="TextBox">
      <Setter Property="Background" Value="#071722"/>
      <Setter Property="Foreground" Value="#D8FBFF"/>
      <Setter Property="BorderBrush" Value="#00E5FF"/>
      <Setter Property="CaretBrush" Value="#00E5FF"/>
      <Setter Property="FontFamily" Value="Consolas"/>
      <Setter Property="Padding" Value="4,2"/>
    </Style>

    <Style x:Key="SpifComboBoxItem" TargetType="ComboBoxItem">
      <Setter Property="Background" Value="#071722"/>
      <Setter Property="Foreground" Value="#D8FBFF"/>
      <Setter Property="FontFamily" Value="Consolas"/>
      <Setter Property="Padding" Value="8,4"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="ComboBoxItem">
            <Border x:Name="Bd" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}">
              <ContentPresenter/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsHighlighted" Value="True">
                <Setter TargetName="Bd" Property="Background" Value="#0D2B3A"/>
                <Setter Property="Foreground" Value="#00E5FF"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style TargetType="ComboBox">
      <Setter Property="Foreground" Value="#D8FBFF"/>
      <Setter Property="Background" Value="#071722"/>
      <Setter Property="BorderBrush" Value="#00E5FF"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="FontWeight" Value="Bold"/>
      <Setter Property="FontFamily" Value="Consolas"/>
      <Setter Property="ItemContainerStyle" Value="{StaticResource SpifComboBoxItem}"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="ComboBox">
            <Grid>
              <Border x:Name="Outer" Background="#071722" BorderBrush="#00E5FF" BorderThickness="1" CornerRadius="6" Padding="6,2" IsHitTestVisible="False">
                <Grid>
                  <Grid.ColumnDefinitions><ColumnDefinition Width="*"/><ColumnDefinition Width="18"/></Grid.ColumnDefinitions>
                  <ContentPresenter Grid.Column="0" Content="{TemplateBinding SelectionBoxItem}" VerticalAlignment="Center" RecognizesAccessKey="True"/>
                  <TextBlock Grid.Column="1" Text="▼" Foreground="#00E5FF" FontSize="10" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Grid>
              </Border>
              <ToggleButton x:Name="ToggleButton" Background="Transparent" BorderThickness="0" Focusable="False" IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" ClickMode="Press"/>
              <Popup x:Name="Popup" Placement="Bottom" IsOpen="{TemplateBinding IsDropDownOpen}" AllowsTransparency="True" Focusable="False" PopupAnimation="Slide">
                <Border Background="#071722" BorderBrush="#00E5FF" BorderThickness="1" CornerRadius="6" MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}">
                  <ScrollViewer MaxHeight="220">
                    <ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained"/>
                  </ScrollViewer>
                </Border>
              </Popup>
            </Grid>
            <ControlTemplate.Triggers>
              <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Outer" Property="Background" Value="#0D2B3A"/></Trigger>
              <Trigger Property="IsDropDownOpen" Value="True"><Setter TargetName="Outer" Property="BorderBrush" Value="#33FF85"/></Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>



    <Style x:Key="SpifToggleSwitch" TargetType="ToggleButton">
      <Setter Property="Width" Value="54"/>
      <Setter Property="Height" Value="24"/>
      <Setter Property="Background" Value="#142A36"/>
      <Setter Property="BorderBrush" Value="#00E5FF"/>
      <Setter Property="Foreground" Value="#D8FBFF"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="ToggleButton">
            <Grid>
              <Border x:Name="Track" CornerRadius="12" Background="#142A36" BorderBrush="#00E5FF" BorderThickness="1"/>
              <Ellipse x:Name="Thumb" Width="18" Height="18" Fill="#D8FBFF" HorizontalAlignment="Left" Margin="3,0,0,0"/>
              <TextBlock x:Name="StateText" Text="OFF" Foreground="#8FEFFF" FontSize="9" FontFamily="Consolas" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,6,0"/>
            </Grid>
            <ControlTemplate.Triggers>
              <Trigger Property="IsChecked" Value="True">
                <Setter TargetName="Track" Property="Background" Value="#0E3A2A"/>
                <Setter TargetName="Track" Property="BorderBrush" Value="#33FF85"/>
                <Setter TargetName="Thumb" Property="Fill" Value="#33FF85"/>
                <Setter TargetName="Thumb" Property="HorizontalAlignment" Value="Right"/>
                <Setter TargetName="Thumb" Property="Margin" Value="0,0,3,0"/>
                <Setter TargetName="StateText" Property="Text" Value="ON"/>
                <Setter TargetName="StateText" Property="Foreground" Value="#33FF85"/>
                <Setter TargetName="StateText" Property="HorizontalAlignment" Value="Left"/>
                <Setter TargetName="StateText" Property="Margin" Value="6,0,0,0"/>
              </Trigger>
              <Trigger Property="IsMouseOver" Value="True">
                <Setter TargetName="Track" Property="BorderBrush" Value="#FFFFFF"/>
              </Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>

    <Style TargetType="TabItem">
      <Setter Property="Padding" Value="14,7"/>
      <Setter Property="Margin" Value="2,0"/>
      <Setter Property="Foreground" Value="#D8FBFF"/>
      <Setter Property="FontFamily" Value="Consolas"/>
      <Setter Property="Template">
        <Setter.Value>
          <ControlTemplate TargetType="TabItem">
            <Border x:Name="Bd" CornerRadius="10,10,0,0" BorderBrush="#00E5FF" BorderThickness="1" Background="#071722">
              <ContentPresenter ContentSource="Header" HorizontalAlignment="Center" VerticalAlignment="Center" Margin="10,4"/>
            </Border>
            <ControlTemplate.Triggers>
              <Trigger Property="IsSelected" Value="True"><Setter TargetName="Bd" Property="Background" Value="#0D2B3A"/></Trigger>
              <Trigger Property="IsMouseOver" Value="True"><Setter TargetName="Bd" Property="BorderBrush" Value="#33FF85"/></Trigger>
            </ControlTemplate.Triggers>
          </ControlTemplate>
        </Setter.Value>
      </Setter>
    </Style>
  </Application.Resources>
</Application>
